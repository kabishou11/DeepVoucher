# 2026-03-19 状态快照

## 完成度（约 95%）
- 真实 ModelScope + 多模态抽取、标准化、组单、规则修正、科目候选召回与预览/阻断全链路已落在 `core/workflows/voucher_pipeline.py`，前端 `task-detail` 工作台涵盖 evidence preview、流程节点、阻断提示与 JSON 导出。
- 数据与知识层已经解析出 PDF/科目表/文档参数，科目召回主链路已接入 LanceDB FTS + 本地 embedding 向量融合，并保留 JSON 搜索兜底。
- 回归与样本稳定策略、阻断机制、人工确认闭环及 `.env`+`requirements`/`package.json` 环境重建的工具链已完成。

## 已验证能力
- 100%准确率要求通过：ModelScope 实时抽取失败或歧义后直接阻断，而非回退示例数据。
- 借贷平衡/末级科目/字段完整校验、残差金额阻断、人工复核与后续 JSON 导出流程全链路在 `core/rules` + `core/workflows` 中覆盖。
- 多借多贷的金额单元拆分可以追溯到证据文件，并在前端以 `amount_notes`、`preview_lines`、`rule_trace` 展示，满足审计需求。
- 知识检索状态现在会暴露 `vector_ready`、`vector_used`、`search_mode`、`embedding_error`，方便在工作台和排障中直观看到当前是否命中了本地 embedding。
- 回归脚本（`scripts/run_current_sample_regression.py`）与 fixtures/测试确保当前样本可稳定生成配置中定义的凭证字段。

## 风险
- LanceDB 权限在此环境下依旧受限（文件重命名失败），向量写入需在主机解锁权限后复核，否则只能“JSON fallback”场景。
- 当前 `venv` 还需要补齐 `sentence-transformers` 运行时依赖，才能真正加载本地 `bge-m3` 模型。
- ModelScope 依赖外部 Token/API，网络或 key 失效会立即阻断导出，需要部署时建重试与告警。
- 100%准确率策略会在任意科目/金额/摘要歧义时阻断，若人工确认体验/说明不清晰，将造成流转延迟。

## 剩余任务
- 在本机正常权限环境补齐 `sentence-transformers`，复验真实 `bge-m3 + LanceDB` 写入、建索引、检索。
- 快照前端/Agent 体验与 `.env` 配置页，准备从“方案1”向更通用“方案3”演进。
- 在权限完整环境重新跑所有回归：`scripts/run_current_sample_regression.py`、`tests/test_task_api.py` 以及 evidence-based JSON 对比。

## 预计交付节奏
1. 抽象规则/测试：1.5 个工作日（设计+实现+回归）。
2. 文档/看板同步：0.5 个工作日（状态报告、阻断记录）。
3. 前端/Agent 感知优化：1 个工作日。
4. 回归验证（含 LanceDB/ModelScope 联通）：1 个工作日（权限恢复后运行）。
