# 当前实现与目标架构差异

日期：2026-03-19

## 已经落地

- FastAPI + Next.js 工作台
- ModelScope API 多模态抽取
- 结构化知识解析
- 规则闸门与人工复核闭环
- 当前样本拆分规则注册表

## 目标中仍在推进

### 1. 检索层

目标：

- 本地 embedding + LanceDB 向量 / 混合检索

当前：

- 结构化 JSON 搜索已可用
- LanceDB 在受限环境下会回退
- embedding 尚未完整进入主召回链路

### 2. 编排层

目标：

- LangGraph / agent 化状态机编排

当前：

- 主链路仍为函数式 pipeline
- 前端已经把中间节点完整可视化

### 3. 规则层

目标：

- 通用票据拆分规则库
- 更丰富的场景规则与解释能力

当前：

- 已从工作流中抽出规则注册表
- 现阶段重点还是先保证当前样本和阻断机制稳定

### 4. 知识增强层

目标：

- 可选知识图谱 / 关系层增强解释和追溯

当前：

- 不是当前阶段必需项
- 后续可在制度条款、事项类型、科目路径之间增加关系建模

## 当前最务实路线

- 先完成方案 1 的交付闭环
- 用更多真实样本把规则库打厚
- 再把 embedding、LanceDB、LangGraph、关系层逐步替换进主干
